# Pinned to the versions this was built and tested against, so that a
# reviewer running `pip install -r requirements.txt` gets the same behaviour.
# Python 3.12 or newer.

# Core UI
streamlit==1.63.0

# LLM / orchestration
langchain==1.4.0
langgraph==1.2.11
langchain-community==0.4.2
langchain-ollama==1.1.0
ollama==0.6.2

# Vector store
chromadb==1.5.9

# Data
pandas==3.0.5

# Tests
pytest==9.1.1
